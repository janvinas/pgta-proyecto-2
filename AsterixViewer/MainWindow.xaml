<ui:FluentWindow x:Class="AsterixViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:AsterixViewer"
        xmlns:tabs="clr-namespace:AsterixViewer.Tabs"
        
        mc:Ignorable="d"
        Title="ASTERIX Decoder"
                 
        Height="600"
        Width="971">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar 
            Title="AsterixParser"
            Grid.Row="0"/>

        <ui:NavigationView 
        PaneDisplayMode="Left"
        IsPaneOpen="False"
        Grid.Row="1">
            <ui:NavigationView.Header>
                <ProgressBar x:Name="ProgressBar" Height="10" Minimum="0" Maximum="1" Visibility="Hidden"/>
            </ui:NavigationView.Header>
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem
                Content="Open File"
                Icon="{ui:SymbolIcon FolderAdd24}"
                Click="OpenFile_click"/>

                <ui:NavigationViewItem
                Content="Table"
                Icon="{ui:SymbolIcon DatabaseStack16}"
                TargetPageType="{x:Type tabs:TableTab}"/>

                <ui:NavigationViewItem
                Content="Map"
                Icon="{ui:SymbolIcon Map24}"
                TargetPageType="{x:Type tabs:MapTab}"/>

                <ui:NavigationViewItem
                Content="Proyecto 3"
                Icon="{ui:SymbolIcon AirplaneTakeOff24}"
                TargetPageType="{x:Type tabs:Proyecto3}"/>
            </ui:NavigationView.MenuItems>

        </ui:NavigationView>
    </Grid>

</ui:FluentWindow>
