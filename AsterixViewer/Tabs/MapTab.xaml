<UserControl x:Class="AsterixViewer.Tabs.MapTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AsterixViewer.Tabs"
             xmlns:map="clr-namespace:AsterixViewer.AsterixMap"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
             d:DataContext="{d:DesignInstance Type=map:MapViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <esri:MapView Grid.Row="0" x:Name="MainMapView"
                      Map="{Binding Map}"
                      GraphicsOverlays="{Binding GraphicsOverlays}">
        </esri:MapView>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="backwards one step" Margin="1px"></Button>
            <Button Content="play/pause" Margin="1px"></Button>
            <Button Content="forward one step" Margin="1px"></Button>
        </StackPanel>
    </Grid>
</UserControl>
